#!/bin/sh

TDAY=`date +%Y%m%d`

mysqldump --default-character-set=binary --hex-blob --host=<%= node['mediawiki']['wgDBserver'] %>  --user=<%= node['mediawiki']['wgDBuser'] %> --password=<%= node['mediawiki']['wgDBpassword'] %> <%= node['mediawiki']['wgDBname'] %> -c > <%= node['mediawiki']['backup_folder'] %>/backup_$TDAY.sql

# cd <%= node['mediawiki']['directory'] %>
# php maintenance/dumpBackup.php --full > backup.xml
# tar zcvf <%= node['mediawiki']['backup_folder'] %>/backup_$TDAY.tar.gz LocalSettings.php images skins extensions  backup.xml
# rm -rf backup.xml

